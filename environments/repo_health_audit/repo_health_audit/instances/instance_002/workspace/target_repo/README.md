# Velocity Payments Engine

The project layout is intentionally non-standard.

## Canonical quality commands
- Test command: `python tooling/qa_runner.py --suite all`
- Coverage command: `python tooling/qa_runner.py --report coverage`

`make verify` and `make coverage` are wrappers around those commands.
