[project]
name = "datasync"
version = "0.1.0"
dependencies = [
    "requests>=2.31.0",
    "pydantic>=2.5.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
